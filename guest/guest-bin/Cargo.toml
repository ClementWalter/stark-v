[package]
name = "guest-bin"
version = "0.1.0"
edition = "2024"

# Binaries are auto-discovered from src/bin/ which are generated by build.rs

[dependencies]
guest-lib = { path = "../guest-lib" }
postcard = { version = "1", default-features = false }
serde = { version = "1", default-features = false }
